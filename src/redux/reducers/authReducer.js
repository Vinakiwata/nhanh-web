
const defaultState= {
    token: '',
    user: null,
};

const authentication = (state = defaultState, action) =>  {
    switch (action.type) {
        case '':
            
            break;
    
        default:
            break;
    }
}

export default authentication;